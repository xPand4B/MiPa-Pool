<img 
    src="{{ asset(config('language.flag_assets'). $code .'.png') }}"
    alt="{{ $name }}"
    class="{{ config('language.flags.img_class') }}"
    width="{{ config('language.flags.width') }}"
/>
